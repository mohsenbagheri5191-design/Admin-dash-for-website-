/** Entry point for the portfolio dashboard page. Kept as a file rather than an
 *  inline <script> so the Content Security Policy can stay script-src 'self'. */
import { boot } from '../views/portfolio.js';
boot();
